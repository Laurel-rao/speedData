# 后端流式上传协议

旧 `/api/upload/start` 保持原协议：开始时提供整文件 SHA256 与完整 `chunk_hashes`。新接口不要求预先扫描文件，调用方可在读取、上传分块的同时累计整文件 SHA256。本次后端实现不修改发送端主流程或页面。

所有接口均为 POST，使用当前接收端 `X-FT-Token` 授权。JSON 接口沿用既有请求大小限制。

## 1. 打开任务

`POST /api/upload/open` 使用与旧 start 相同的 JSON 外壳：

```json
{
  "source": "64位十六进制来源标识",
  "manifest": {
    "path": "folder/file.bin",
    "size": 4194310,
    "mtime": 1700000000,
    "chunk_size": 4194304,
    "n_chunks": 2
  },
  "extract": false,
  "files": 0,
  "bytes": 0
}
```

`size` 必须已知，分块大小固定为 4 MiB，`n_chunks` 为向上取整后的块数，空文件为 0。单文件上限沿用 1 TiB。`sha256` 与 `chunk_hashes` 可省略；如提前提供整文件 `sha256`，所有块收到后自动校验并完成。否则必须调用 finish。

返回现有 status 结构，包括 `id`、`path`、`generation`、`missing`、`missing_chunks`、`done_chunks` 和 `phase`。`missing` 每次最多返回 64 个块索引，按状态接口继续查询。相同来源、路径、大小、修改时间的任务重复 open 会复用任务；活动任务的元数据冲突会拒绝。已完成任务可通过不同元数据打开新版本。open 可以复用元数据一致的旧协议任务，并保留其原始整文件和分块哈希约束；旧 start 不可覆盖活动流式任务。源文件改变但大小和修改时间不变时，应使用新的 source 标识。

## 2. 上传分块

`POST /api/upload/chunk?id=...&path=...&generation=...&index=...&chunk_sha256=...`

请求体为分块内容，`X-Enc: r` 为原始数据，`X-Enc: z` 为 zlib 压缩数据。新任务必须提供 `chunk_sha256`，其值对应**解压后的原始块**，接受大小写十六进制。索引、长度、解压上限、SHA256 即时校验通过后才写入暂存文件并同步到磁盘，然后原子持久化已完成块及其哈希；返回 200 表示该块已保存。相同块重复上传幂等，不同内容的重复块被拒绝。旧任务不需要新增此参数；如果提供，也会校验它。

## 3. 完成并验证

```json
{
  "id": "open返回的id",
  "path": "folder/file.bin",
  "generation": 0,
  "sha256": "按文件原始字节顺序累计的整文件SHA256"
}
```

将上述 JSON 提交到 `POST /api/upload/finish`。所有块齐备后才接受；返回 200 表示最终哈希已持久化并提交后台校验，**不表示文件已经完成**。继续调用原 `/api/upload/status` 直到 `status=done`；校验失败会返回 `status=error`，不会发布未经验证的文件。整文件校验失败会清除分块完成记录及最终哈希，恢复任务后需重新上传。压缩包也在整文件校验成功后才进入既有解压流程。同名策略沿用 rename/overwrite/skip；skip 跳过现有文件，不校验或覆盖其内容。

未提交最终哈希时，即使分块齐备也保持 `phase=waiting_for_finish`。空文件同样需要 finish，除非 open 已提供空文件 SHA256。

## 持久化与边界

- 任务清单及分块元数据保存在接收目录的受保护 `.filetransfer-cache`。重启后重新认证并以相同 source 与元数据调用 open，会恢复该来源的未完成任务；恢复时重新读取已保存块并校验其持久化哈希，损坏块重新列入 missing。
- finish 已持久化、但校验尚未结束的任务也会在重新 open 后继续整文件校验。暂停/取消后的旧 generation 会被拒绝，继续操作前重新查询状态。
- 沿用旧后端的任务生命周期：完成任务从持久化队列移除，因此重启后的已完成任务不提供持久化幂等回执；rename 策略下重新提交已完成任务可能产生同名副本。暂停/错误状态本身不跨重启保留，未完成任务重新 open 后可继续。
- source、大小与修改时间是流式任务身份的一部分，不能用于识别“相同大小、相同时间但内容改变”的文件。调用方应在源文件改变时使用新 source，最终 SHA256 仍是发布前的完整性门槛。
- 文件与元数据均 fsync/原子替换，但目录项未额外 fsync；覆盖进程退出/重启恢复，不承诺断电时的文件系统持久性。没有新增发送端协商或 UI。
