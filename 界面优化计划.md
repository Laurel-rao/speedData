# FileTransfer 页面信息展示优化计划

> 目标：不只改样式，而是重构「信息如何呈现」——让用户一屏内看懂「在传什么、传了多少、还剩多少、有没有问题」，操作路径最短，手机与桌面体验一致。

## 一、现状问题清单

### A. 信息层（展示什么）
| # | 问题 | 位置 | 影响 |
|---|------|------|------|
| 1 | 只有平均速度数字，无趋势、无 ETA | 三个面板 stats | 无法预判还要等多久 |
| 2 | 文件列表扁平铺路径全名，无目录层级 | sendTree / monTree / recvFiles | 多文件无法定位，长路径挤爆单行 |
| 3 | 状态徽章是英文 raw 值 | 所有列表 | 非技术用户看不懂 pending/verifying |
| 4 | 「12/40 块」暴露内部实现 | recvFiles | 用户不知道「块」是什么 |
| 5 | 压缩率只有 1.27x 裸数字 | sendStats / monStats | 无「节省了多少」的上下文 |
| 6 | 发送方列表=共享清单，不反映已传/未传 | sendTree | 发送方自己看不出推送进度 |
| 7 | 日志是原始文本，无时间/结果/筛选 | 工具卡片 | 排查问题靠肉眼扫 |
| 8 | 测试连接结果只有一行小字 | sendMsg | 失败原因不显眼、无引导 |

### B. 界面/交互层（怎么展示）
| # | 问题 | 影响 |
|---|------|------|
| 1 | 每秒整卡 innerHTML 重绘（sendTick/recvTick/monTick） | 列表闪烁、滚动丢失、观感差、浪费性能 |
| 2 | 地址+密码无复制按钮 | 手机端手动抄写 http://IP:8600 + 密码，易错 |
| 3 | 按钮无禁用/loading 态 | 可重复点击造成重复提交 |
| 4 | 提示全用一行小字，成功/失败无视觉差异 | 关键结果（如测试连接）被淹没 |
| 5 | 固定 640px，无响应式断点 | 手机窄屏列表挤压、横向溢出 |
| 6 | 空状态、断线状态无提示 | 等待/中断时无方向感，轮询失败静默 |
| 7 | 轮询不感知页面可见性 | 后台标签也每秒拉取，耗电耗流量 |

## 二、优化原则

1. **一屏三问**：页面打开能立刻回答「在传什么 / 传了多少剩多少 / 有没有问题」。
2. **结论前置、明细分层**：总览卡（结论）→ 文件列表（明细）→ 日志（追溯）。
3. **状态语义化**：中文状态 + 彩色徽章，错误醒目可点重试。
4. **少重绘、多增量**：keyed DOM 差量更新 + CSS 过渡，杜绝闪烁。
5. **移动优先**：窄屏默认可用，宽屏增强信息密度。

## 三、分阶段实施方案

### Phase 1 数据层（后端小改 transfer.go）
- [x] `Puller.state()` / `SenderStats.snapshot()` 增加 `elapsed`、`compressed_saved`（raw−wire）字段
- [x] 日志输出结构化 JSON 条目（time / file / status / size / err），前端直接渲染，不再裸文本
- [x] （可选）`/api/push-test` 返回链路质量信息（耗时、校验结果徽章）

### Phase 2 信息展示重构（前端核心，index.html）
- [x] **概览卡升级**：总进度 + 瞬时速度 sparkline（前端采样 60 点画 SVG）+ ETA + 已用时间 + 压缩节省量显示为「节省 320 MB（2.1x）」而非裸数字
- [x] **文件列表重构**：
  - 按状态分组（传输中 / 排队 / 已完成 / 失败 / 暂停 / 校验中），分组可折叠
  - 中文状态徽章五色：进行中蓝 / 完成绿 / 失败红 / 暂停黄 / 排队灰
  - 目录树缩进展示（按根目录分组），长路径省略号、点击可看全名
  - 每文件：进度条 + 已传/总量 + 单文件 ETA + 错误原因（失败行红底、可点「重试」）
  - 移除「块」这类内部词汇，改显百分比与大小
- [x] **接收端地址卡**：地址 + 密码一键复制（navigator.clipboard，失败降级为选中文本）
- [x] **日志卡**：结构化条目渲染（时间戳 / 文件 / 结果徽章），筛选 全部 / 成功 / 失败
- [x] **完成汇总卡**：全部完成后显示 文件数 / 总量 / 耗时 / 平均速度
- [x] **测试连接结果增强**：成功显示校验徽章，失败给出检查清单（地址格式 / 防火墙 / 对方是否启动）

### Phase 3 界面与交互打磨
- [x] keyed DOM 差量渲染：列表行按数据 key 更新，进度条 CSS transition，滚动不重置
- [x] 响应式断点 @media 560px：统计卡 2 列、行内信息简化、按钮全宽
- [x] 按钮 loading / 禁用态：发送中禁用「发送」，操作按钮带反馈
- [x] Toast 组件：成功 / 错误 / 信息三色，顶部滑入自动消失
- [x] 空状态与断线提示条：「与对方连接中断，正在重试…」红色状态条
- [x] visibilitychange：页面隐藏时暂停轮询，恢复后立即刷新一次
- [x] tab 切换与分组折叠的微动画

### Phase 4 验证与交付
- [x] `go build` 通过，本机 8600 起服，发送 / 接收 / 监控三条路径手测
- [x] 手机窄屏视口测试（Chrome 设备模拟）
- [x] 重新打包 filetransfer.exe / filetransfer-arm64.exe，确认 go:embed 生效
- [x] 断点续传恢复后的 UI 状态冒烟测试

## 四、风险与取舍

- **二维码（可选）**：地址+密码可加纯 JS 二维码（无依赖 ~2KB），不加不影响核心能力
- **sparkline 数据**：前端每秒采样即可，无需后端新接口
- **后端改动最小化**：仅日志结构化和压缩节省量两处小改，其余全在前端
- **clipboard 兼容**：navigator.clipboard 需 localhost/https 上下文，本工具恰好是 localhost / 局域网，可用；失败降级 execCommand

## 五、验收标准

1. 打开页面 3 秒内能说清「传了几个、剩几个、还需多久」
2. 文件列表滚动不闪烁，状态即时准确
3. 手机宽度下无横向滚动，所有操作可点
4. 出错/失败文件一眼可见且有重试入口
5. 全部完成后有汇总卡；刷新/重开不丢失主状态（localStorage 已有基础）

---

## 六、实施记录（2026-09-11 完成）

实际实现与计划的差异 / 补充：

1. **后端改动仅 3 处**（transfer.go）：`SenderStats.snapshot()` 增加 `compressed_saved`；`Puller` 增加 `startedAt` 字段并在 `state()` 返回 `elapsed`；其余不需要改——日志在磁盘上本就是 JSON 行，前端直接解析渲染即可。
2. **文件列表** 采用「状态分组 + 目录树缩进」双维度（组头可折叠，已完成默认折叠），每行含中文徽章、进度条、已传/总、单文件 ETA、错误原因与重试按钮；彻底移除「块」内部词汇。
3. **概览卡**：总进度 + 速度趋势 sparkline（前端 60 点采样）+ 预计剩余 (ETA) + 已用时 + 压缩节省量（显示「节省 X」而非裸压缩率）。
4. **连接恢复（修复用户报的死胡同）**：刷新后不再隐藏发送配置卡；改为「已连接接收方（进度已恢复）」横幅 + 配置卡保持可见 + 「再发文件夹」行；token 失效时自动露出配置区并停轮询，重新发送自动恢复轮询。文件总数/压缩状态持久化到 localStorage。
5. **保留并整合了用户在中间版本里加的功能**：粘贴识别（sendPaste/parseConn）、压缩传输开关（compressOn）、「复制地址+密码」合并复制、发送统计（文件总数/传输中/完成量）。
6. **差量渲染** diffList：按 key 只更新变化的行，进度条 CSS transition，列表滚动不重置、不闪烁。
7. **交互**：Toast 反馈、按钮 loading/禁用、断连横幅（仅"曾连接过"才显示）、页面隐藏暂停轮询/恢复立即补拉、复制按钮（地址/密码/路径/合并）。
8. **验证结果**：go build/vet/gofmt/test 全过；双实例端到端实测（压缩传输→接收端解压→哈希一致）；发送/接收/监控三视图；刷新恢复；两个 Windows exe 已重新打包且内含新页面。
9. 未做（可选项）：二维码、断点续传 UI 冒烟（后端逻辑未动，风险低）。
